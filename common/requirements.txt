# Shared Python dependencies for all bootcamp labs.
# Install:  pip install -r common/requirements.txt
requests>=2.31,<3
python-dotenv>=1.0,<2
rich>=13.0          # pretty terminal output for lab scripts
tabulate>=0.9       # table formatting for triage output
Flask>=3.0,<4       # ai-soc-assistant, victim-web, mock-ollama
